# Hooks

This plugin does not enable hooks by default.

Reason:
- Hooks run automatically and can have surprising side effects.
- Intent++ is designed around manually-invoked skills with explicit phase gates.

If you want to experiment with hooks, use `hooks/hooks.example.json` as a starting point.
Copy it to `hooks/hooks.json` to enable.
